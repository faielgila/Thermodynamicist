<ContentDialog
	x:Class="ThermodynamicistUWP.Dialogs.ErrorDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:ThermodynamicistUWP.Dialogs"
	xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="Calculation Error"
	CloseButtonText="Cancel calculation">

	<StackPanel>
		<TextBlock Text="{x:Bind ErrorText}" IsTextSelectionEnabled="True"/>
		<TextBlock Text="Stack trace: "/>
		<ScrollViewer Padding="0, 0, 20, 20"
					  VerticalScrollBarVisibility="Auto"
					  VerticalScrollMode="Enabled"
					  HorizontalScrollMode="Enabled"
					  HorizontalScrollBarVisibility="Auto">
			<TextBlock Text="{x:Bind ErrorStacktrace}" IsTextSelectionEnabled="True"/>
		</ScrollViewer>
	</StackPanel>
</ContentDialog>
